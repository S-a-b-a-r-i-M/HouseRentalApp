<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />

    <!--
    This(android:name=".presentation.ui.MyApplication") tells Android:
        "Use MyApplication class as the Application instance for this entire app"
        The . prefix means it's relative to your package name (e.g., com.example.app.presentation.ui.MyApplication)
        This class will be instantiated BEFORE any other components (Activities, Services, etc.)
    -->
    <application
        android:name=".presentation.ui.MyAppApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_home"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_home_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.MySplashScreen">
        <activity
            android:name=".presentation.ui.property.CreatePropertyActivity"
            android:exported="false"
            android:documentLaunchMode="intoExisting" />
        <activity
            android:name=".presentation.ui.MainActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.auth.AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>